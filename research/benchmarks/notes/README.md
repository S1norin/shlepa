# Benchmark notes

One digest per benchmark (or benchmark-related paper): `<slug>.md`, using the
template in [`../README.md`](../README.md) → "Note template".

Sections are mandatory: *What it tests*, *Environment*, *Tasks*, *Scoring*,
*Fit for Shlepa*, *Sources*. Slugs match the PDF slug where a paper exists.
