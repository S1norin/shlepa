# Papers

Registry of research PDFs. Store the file as `<slug>.pdf` and add one row per
paper. Use the `pdf-reading` pi skill (liteparse) to extract text and write a
digest to `../notes/<slug>.md`.

| slug | title | link | why (one line) |
| ---- | ----- | ---- | -------------- |
