schema_version = "1.2"

[task]
name = "bench/vulngym-langchain-template-injection"
description = "Audit a LangChain template-rendering source snapshot and report the unsafe traversal path."
authors = []
keywords = []

[metadata]
difficulty = "hard"
category = "security"
tags = ["bench", "vulnerability-detection", "source-audit", "python", "template-injection"]
source = "Tencent/VulnGym"
url = "https://github.com/Tencent/VulnGym/tree/v0.1.4"
reference = "https://github.com/advisories/GHSA-6qv9-48xg-fc7f"
synced_at = "2026-09-05"

[verifier]
timeout_sec = 120.0
[agent]
timeout_sec = 600.0
[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = false
mcp_servers = []
[verifier.env]
[environment.env]
[solution.env]

# #128: sink-keyword check — the report's critical_operation window must
# contain one of these; catches reports that trace the source side instead.
[deliverable_check]
keywords = "getattr|__getitem__|format|render"
