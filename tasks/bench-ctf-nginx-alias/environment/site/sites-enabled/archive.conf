server {
    listen 8080;
    root /app/site/public;

    location /assets/ { try_files $uri =404; }

    # Intended for administrators only, but no access rule was carried over.
    location /maintenance/ {
        alias /app/site/private-backups/;
        autoindex on;
    }
}
