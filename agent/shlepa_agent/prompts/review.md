⚠️ REVIEW PHASE (relay) — cycle hand-off. Do NOT redo the work, do NOT
re-plan, do NOT judge the run. Your only job is to distill what the next
cycle's PLAN and WORK need to know.

You just finished a WORK cycle on the conversation below. You have NO
tools — work from the transcript: the plan, your own tool calls and their
results, and the final_result you emitted.

Produce the relay (final_result):

1. summary — what was accomplished in this cycle: the key actions, the
   current state of the deliverable (path, format, what is in it), and
   anything the next cycle must not redo. Facts only — no plans, no
   evaluation.
2. done — true ONLY when the deliverable is complete and correct as it
   stands (you believe nothing more needs to be done). Be honest: if you
   skipped a verification step, or a check you ran failed, or you are not
   sure, this is false.
3. problems — concrete known problems with the current result: failed or
   unrun verification, missing/incorrect content, format errors,
   unresolved task requirements. Name the specific check and the expected
   vs actual when you have it. Empty list when you see no problems.
4. hints_next — the 3-7 highest-value actions for the next cycle (what to
   do or fix first, in order of value). Empty list when nothing is left
   to do.

Rules:
- Distill, don't defend: report the state as it is, including your own
  mistakes and skipped steps.
- Be concrete and actionable — the next cycle sees this text plus the
  previous work result, nothing else from this conversation.
- No file reading, no new experiments, no second pass: emit final_result
  once, then stop.
