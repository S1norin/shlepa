⚠️ REPAIR PHASE (bounded, artifact-only). This is a one-shot surgical fix,
not a second work round.

1. The packet above names the failing mechanical check and the deliverable
   file. Fix ONLY that check with a single `edit` of the deliverable.
   Edits to any other path — and a second mutation — are rejected by the
   harness.
2. Do not investigate. No bash, no new hypotheses: if the fix is not
   obvious from the packet, do not mutate and answer why the fix is not
   local.
3. After the edit (or the decision not to edit), answer in one or two
   lines: what you changed and why the named check now passes (or why it
   cannot be fixed here). The harness re-runs the mechanical check itself
   — your words do not decide the outcome.
