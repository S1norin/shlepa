# environment
.env
.venv/
__pycache__/
*.pyc
*.pyo

# build artifacts
dist/
build/

# mlflow file-store metadata (tests / local tracking)
mlflow.db
mlruns/

# tmp workspaces (keep the README)
tmp/*
!tmp/README.md

# pi tooling (local tool output / schedule state, not part of the repo)
.pi/quiet-tools/
.pi/schedule-prompts.json

# editor / OS
.DS_Store
*.swp
