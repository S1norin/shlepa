# known_issues/

One markdown file per known issue or triaged failure:
`YYYYMMDD-<short-slug>.md` (date first observed). Keep them short: symptom,
repro, impact, workaround, and the related GitHub issue if one exists.
