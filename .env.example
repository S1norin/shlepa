# Shlepa environment configuration.
# Copy to .env and fill in real values. .env is gitignored.

# --- Main LLM endpoint (experiments / dev runs) ---
OPENAI_BASE_URL=
OPENAI_API_KEY=
LOCAL_AGENT_MODEL=

# --- Secondary (weaker) endpoint for CI ---
CI_OPENAI_BASE_URL=
CI_OPENAI_API_KEY=
CI_MODEL=

# --- MLflow tracking ---
MLFLOW_TRACKING_URI=
MLFLOW_TRACKING_USERNAME=
MLFLOW_TRACKING_PASSWORD=

# --- Telemetry (dev only; always stripped from the submission zip) ---
# 1 enables OpenTelemetry span export, empty/0 disables it
SLEPA_OTEL_ENABLED=
# OTLP/HTTP endpoint of the local collector (see otel/README.md)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# Max bytes per span attribute value (default 8192). Full tool stdout and
# conversation messages are embedded in span attributes; the cap keeps
# exported traces small. Standard OTEL_SPAN_ATTRIBUTE_VALUE_LENGTH_LIMIT
# wins when set.
SLEPA_OTEL_ATTR_LIMIT=
# Optional legacy trace experiment (id or name). New CLI runs route each
# trace to the same family experiment as its MLflow run. This value remains
# a fallback for ad-hoc runs, old batches, and the direct doctor OTLP probe.
MLFLOW_TELEMETRY_EXPERIMENT_ID=
# Family experiment for 'otel/check_trace.py --backend mlflow'. The checker
# requires this value or an explicit --experiment argument.
SLEPA_TRACE_EXPERIMENT=
# Note: SLEPA_BATCH_ID / SLEPA_PRESET / SLEPA_GIT_SHA / SLEPA_AGENT_VERSION
# are set by the CLI automatically for each dev run (root-span attributes);
# no user action needed. SLEPA_TASK_SLUG is set per task by the run engine.

# --- Code-search engine (dev only) ---
# The baseline ships code_search + file_outline ON with the rg engine;
# this switch only pins the engine ("rg" = ripgrep, "sifs" = bundled SIFS
# binary). Unset / invalid: the packaged baseline (rg) stays as-is.
# Not wired through the CLI run engine yet (planned with the toolset
# arms, issue #51); for manual container checks pass it to the agent
# process directly.
# AGENT_CODE_SEARCH=
